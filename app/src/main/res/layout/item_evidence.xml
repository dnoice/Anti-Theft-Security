<!-- File: app/src/main/res/layout/item_evidence.xml -->

<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/card_background"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Main Content -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <!-- Image Preview Container -->
            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:layout_marginBottom="8dp">

                <!-- Image Preview -->
                <ImageView
                    android:id="@+id/imagePreview"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:background="@color/background_secondary"
                    android:contentDescription="@string/content_desc_evidence_photo" />

                <!-- File Type Icon -->
                <ImageView
                    android:id="@+id/iconFileType"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_alignParentTop="true"
                    android:layout_alignParentEnd="true"
                    android:layout_margin="8dp"
                    android:src="@drawable/ic_camera"
                    android:background="@drawable/file_type_icon_background"
                    android:padding="4dp"
                    android:contentDescription="@string/content_desc_file_type_icon" />

                <!-- Play Icon for Videos -->
                <ImageView
                    android:id="@+id/iconPlay"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_centerInParent="true"
                    android:src="@drawable/ic_play_circle"
                    android:visibility="gone"
                    android:contentDescription="Play Video" />

            </RelativeLayout>

            <!-- File Information -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- File Name -->
                <TextView
                    android:id="@+id/txtFileName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="security_front_20241213_143521.jpg"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:layout_marginBottom="2dp" />

                <!-- File Info (Date and Size) -->
                <TextView
                    android:id="@+id/txtFileInfo"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Dec 13, 14:35 • 1.2 MB"
                    android:textSize="10sp"
                    android:textColor="@color/text_secondary"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:layout_marginBottom="4dp" />

                <!-- Session Info -->
                <TextView
                    android:id="@+id/txtSessionInfo"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Motion Detection"
                    android:textSize="10sp"
                    android:textColor="@color/security_info"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:background="@drawable/session_info_background"
                    android:padding="2dp"
                    android:visibility="gone" />

            </LinearLayout>

        </LinearLayout>

        <!-- Selection Overlay -->
        <View
            android:id="@+id/selectionOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/selection_inactive"
            android:visibility="gone" />

        <!-- Selection Checkmark -->
        <ImageView
            android:id="@+id/iconSelected"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentTop="true"
            android:layout_alignParentStart="true"
            android:layout_margin="8dp"
            android:src="@drawable/ic_check_circle"
            android:tint="@color/success_color"
            android:visibility="gone"
            android:contentDescription="Selected" />

        <!-- Evidence Quality Indicator -->
        <LinearLayout
            android:id="@+id/layoutQualityIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_margin="8dp"
            android:orientation="horizontal"
            android:background="@drawable/quality_indicator_background"
            android:padding="4dp"
            android:visibility="gone">

            <ImageView
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:src="@drawable/ic_star"
                android:tint="@color/warning_color" />

            <TextView
                android:id="@+id/txtQuality"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="HD"
                android:textSize="8sp"
                android:textColor="@android:color/white"
                android:textStyle="bold"
                android:layout_marginStart="2dp" />

        </LinearLayout>

    </RelativeLayout>

</androidx.cardview.widget.CardView>
